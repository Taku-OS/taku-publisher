# Taku Cursor marketplace bundle

This generated bundle contains `.cursor-plugin/marketplace.json` and the
self-contained `plugins/taku-publisher` Agent plugin. To enable GitHub import,
publish this bundle at a repository or supported ref with the marketplace
manifest at its root; do not point users at source-only `adapters/cursor`.

The production combined distribution is on the GitHub `marketplace` branch.
This is an Agent plugin, not an official Cursor store listing or a VSIX extension.
