# `@taku/creator-core`

This private workspace package is a deprecated compatibility alias for
`@taku/passport-core`. It no longer imports implementation from
`creator/scripts`.

New consumers must use `@taku/passport-core`. Host discovery and effectful
Creator runtime entrypoints remain under `creator/scripts` until P4 introduces
formal Host Adapters.
