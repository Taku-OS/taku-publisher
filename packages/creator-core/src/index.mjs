/**
 * Deprecated compatibility name.
 *
 * New consumers should import @taku/passport-core. Host discovery and I/O
 * remain available from the legacy creator runtime until P4 Host Adapters
 * replace those paths.
 */
export * from '@taku/passport-core';
